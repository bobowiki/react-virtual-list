# ReactVirtualList 虚拟列表

`ReactVirtualList` 是一个 **高性能、轻量级的虚拟滚动列表组件**，  
通过只渲染可视区域内的元素，大幅降低大数据量列表的 DOM 数量，从而提升滚动和渲染性能。

适用于以下场景：

- 超长列表（上千 / 上万条数据）
- 固定高度列表项
- 滚动性能敏感页面
- 后台系统 / 表格 / 日志流 / 消息列表

---

## 特性

- 🚀 高性能虚拟滚动
- 📏 固定高度 + 可配置间隔
- 🔍 支持 overscan 预渲染
- 🧩 Render Props 渲染模式，灵活度高
- 🧠 requestAnimationFrame 合并滚动更新，避免频繁 setState
- 🎯 支持自定义 itemKey

---


## API

### ReactVirtualList Props

`ReactVirtualList` 接收一个 Props 对象，用于控制虚拟列表的渲染行为。

#### Props 说明

| 参数名 | 说明 | 类型 | 必填 | 默认值 |
|------|------|------|------|--------|
| list | 列表数据源 | `any[]` | 是 | - |
| itemHeight | 单个列表项高度（px） | `number` | 是 | - |
| itemGap | 列表项之间的间隔（px） | `number` | 否 | `0` |
| overscan | 可视区域上下额外渲染的项数 | `number` | 否 | `5` |
| className | 容器自定义类名 | `string` | 否 | - |
| style | 容器自定义样式 | `React.CSSProperties` | 否 | - |
| itemKey | 列表项唯一 key 生成函数 | `(item: any, index: number) => React.Key` | 否 | - |
| children | 列表项渲染函数（Render Props） | `(item: any, index: number) => React.ReactNode` | 是 | - |

---

### children 渲染函数

```js
(item: any, index: number) => React.ReactNode
```

* `item`：当前列表项数据
* `index`：当前项在原始列表中的索引

---

### itemKey 说明

当列表数据量较大或列表项可能发生重排时，建议提供 `itemKey`，以避免不必要的重新渲染。

```js
(item, index) => item.id
```

---

## Demo

<code src="../react-virtual-list/example/index.tsx"></code>
